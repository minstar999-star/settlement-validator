# 결산질의 validator 서비스 기획서 (PRD)

- 작성자: 정민규
- 작성일: 2026-08-03

---

## 1. 추진 배경 (문제 정의)
*누가, 어떤 업무에서, 어떤 불편을 겪는가*

- 재정경제부 회계결산과는 국회 예산정책처·예산결산위원회가 국가결산보고서를 근거로 제출하는 정부 예산 결산 관련 질의를 접수한다.
- 담당 실무자는 질의서에 인용된 수치가 적정한지를 D-Brain에서 내려받은 OLAP 자료와 피벗테이블로 대조해 검증해야 한다.
- 이 대조는 항목마다 사람이 일일이 맞춰보는 반복 노동이어서 업무 부담이 크다.
- 특히 이미 확정된 결산 수치를 다시 확인하는 **이중 검산**의 성격을 띠고 있어, 검증 결과에 대해 담당자가 느끼는 심리적 부담이 크다.
- 또한 수치가 어긋나더라도 그 원인이 회계기준 적용 차이 때문인지 단순 오류인지 판단하기 어렵다.

## 2. 현행 방식과 한계
*지금은 이 일을 어떻게 처리하고 있고, 무엇이 아쉬운가*

**현행 방식**

국회 질의서(한글파일) 접수 → 질의서에 인용된 수치 확인 → D-Brain에서 OLAP 자료(엑셀) 내려받기 → 엑셀에서 항목별로 피벗테이블 구성 → 질의서 수치와 눈으로 대조 → 차이가 나면 기준을 바꿔가며 재집계해 원인 추정

**한계**

- 질의 항목 수만큼 피벗테이블을 반복 구성해야 해 시간과 노동이 많이 든다
- 대조를 사람 눈에 의존해 누락·착오 가능성이 있다
- 불일치 원인을 찾는 과정이 담당자의 경험과 감에 의존하고, 판단 근거가 문서로 남지 않는다
- 예결위 위원별로 제출된 질의가 서로 중복되거나, 양식만 다를 뿐 실제로는 같은 계정을 묻는 경우가 많다. 이를 걸러내려면 사람이 일일이 기준표를 만들어 비교해야 해서 과도한 노동이 요구된다
- 검증 과정과 결과가 축적되지 않아, 비슷한 질의가 반복돼도 재활용이 어렵다

## 3. 목표와 기대효과 (성공 기준)
*측정 가능한 숫자로 (처리 시간, 문의 건수, 정답률 등)*

> **"1항목"의 정의** — 질의서에서 뽑아낸 **검증 대상 수치 1개**를 1항목으로 센다.
> 질의 1건에 검증할 수치가 3개 들어 있으면 3항목으로 계산한다.

| 지표 | 현재 | 목표 |
| --- | --- | --- |
| 1항목당 검증 소요 시간 (숙련자) | 약 1일 | 1시간 이내 |
| 1항목당 검증 소요 시간 (신규자·인턴) | 최대 2.5일 | 1시간 이내 |
| 숙련도에 따른 소요 시간 격차 | 최대 2.5배 | 1.2배 이내 |
| 중복·유사 질의 통합으로 줄어드는 실제 검증 대상 항목 수 | 0% (전량 개별 검증) | 30% 이상 감소 |
| 수치 대조 정확도 (담당자 수기 검증 결과와 일치) | — | 95% 이상 |
| `확인불가` 비율 (자동으로 근거를 찾지 못한 항목) | — | 10% 이하 |

- 부수 효과: 이중 검산에 따르는 담당자의 심리적 부담을 완화하고, 판단 근거를 화면에 남겨 검증 결과의 설명 가능성을 높인다.

## 4. 사용자와 이용 흐름
*누가 쓰는지와 사용 순서 (화살표로)*

**사용자**

- 재정경제부 회계결산과 등 국회 질의에 대응해 결산 수치를 검증하는 부처 실무 담당자
- 숙련 담당자와 신규 담당자·인턴이 모두 같은 절차로 사용한다

**이용 흐름**

국회 질의서(한글파일)를 PDF로 변환 → 질의서 PDF 업로드 → D-Brain OLAP 자료(엑셀) 업로드 → 두 파일 모두 "준비 완료" 확인 → 검증 실행 → **질의 항목 선별**(검증 대상 수치·계정 추출, 금액 수치가 없는 질의는 `검증대상외`로 분류) → 항목별 판정표(`일치` / `불일치` / `확인불가`)와 근거 위치 확인 → `불일치` 항목의 회계기준 원인·근거 확인 → `검증대상외` 목록을 훑어 누락 여부 확인 → 결과 표를 복사해 담당자가 최종 판단으로 회신 자료를 직접 작성

## 5. 주요 기능 (must / nice로 구분)
*반드시 있어야 할 기능(must)과 있으면 좋은 기능(nice)을 나눠서*

### [must] 1) 수치 대조 검증

- 설명: 질의서에서 검증 대상 수치와 대상 계정을 먼저 선별한 뒤, 그 수치를 OLAP 자료의 실제 수치와 항목별로 자동 대조해 일치 여부를 표로 보여준다.
- AI가 꼭 지켜야 할 규칙:

  **(가) 파일 업로드**

  - 질의서는 PDF, OLAP 자료는 엑셀 파일로 각각 업로드하며, 두 파일이 모두 올라오기 전에는 검증을 시작하지 않는다
  - 질의서 PDF는 여러 개 올릴 수 있고, OLAP 엑셀은 한 번에 1개만 올린다
  - OLAP 엑셀은 첫 행을 머리글로 보고 `회계`·`관`·`항`·`목` 열과 금액 열을 찾는다. 이 중 하나라도 없으면 검증을 시작하지 않고 **어떤 열이 없는지** 화면에 알려준다
  - 처리 상한은 질의서 PDF 합계 50페이지, 검증 대상 수치 100건, OLAP 엑셀 5,000행으로 한다. 상한을 넘으면 처리하지 않고 상한을 안내한다
  - PDF에서 추출한 글자 수가 100자 미만이면 텍스트 추출 실패로 보고, "이미지로 저장된 PDF일 수 있으니 글자가 포함된 PDF로 다시 변환해 올려달라"고 안내한다

  **(나) 질의 항목 선별** — 대조에 앞서 무엇을 검증할지 먼저 정한다

  - 질의서를 질의 항목 단위로 나누고, 각 항목에서 검증 대상 수치를 뽑는다
  - 검증 대상 수치는 금액 표기(원·만원·억원·조원 등)를 가진 수치로만 한정한다. 회계연도, 조문·항목 번호, 비율(%), 건수는 검증 대상으로 삼지 않는다
  - `88조 4,000억원`처럼 조·억·만이 섞인 한글 혼합 표기도 하나의 금액으로 인식한다
  - 각 수치마다 대조할 계정을 질의서 본문에 나타난 회계·관·항·목 표현으로 특정한다
  - 같은 목 이름이 여러 회계에 존재할 수 있으므로, **회계 구분이 확인되지 않으면 임의로 고르지 말고** `확인불가`로 표시하고 후보 계정들을 함께 보여준다
  - 계정을 특정할 단서가 부족하면 임의로 고르지 말고 `확인불가`로 표시하고, 어떤 단서가 부족한지 함께 적는다
  - 질의서가 그 수치를 어느 기준(예산 기준 / 결산 기준)으로 인용했는지도 함께 뽑는다. 기준이 명시되어 있지 않으면 추정하지 말고 `기준 미표기`로 적는다
  - 금액 수치가 없는 질의(자료 제출 요구, 정책 의견 질의 등)는 `검증대상외`로 분류하고, 판정 3종을 적용하지 않는다
  - `검증대상외`로 분류한 질의도 결과 목록에 그대로 남겨, 담당자가 누락 여부를 직접 확인할 수 있게 한다

  **(다) 대조 판정** — (나)에서 검증 대상으로 선별된 수치에만 적용한다

  - 판정은 `일치` / `불일치` / `확인불가` 3가지로만 표시하고, 그 외 표현은 쓰지 않는다
  - OLAP 자료에서 해당 수치를 찾지 못하면 추정하지 말고 반드시 `확인불가`로 표시한다
  - 단위(조원·억원·백만원·원)가 다르거나 혼합 표기이면 원 단위로 환산해 비교하고, 환산 과정을 함께 표시한다
  - **두 수치의 절대차가 10원 미만이면** 절사·반올림에 따른 오차로 보고 `일치`로, **10원 이상이면** `불일치`로 판정한다
  - 질의가 상위 계층(회계·관·항)의 합계를 물으면 해당 하위 `목` 금액을 합산해 비교하고, **합산해서 비교했다는 사실과 합산에 쓰인 행 수를 함께 표시**한다
  - 모든 판정에는 근거 위치를 **회계·관·항·목 4단계와 엑셀 행 번호**로 함께 표시한다
  - 선별된 검증 대상 수치 중 `확인불가` 비율이 80% 이상이면, 개별 판정과 함께 "업로드한 원자료가 질의서와 맞지 않을 수 있다"는 경고를 화면 위에 표시한다

### [must] 2) 불일치 원인(회계기준) 분석

- 설명: `불일치`로 판정된 항목만 골라, 어떤 회계기준 차이 때문에 수치가 어긋났는지 단답형 근거를 제시한다.
- AI가 꼭 지켜야 할 규칙:
  - `불일치`로 판정된 항목에 대해서만 실행한다 (`일치`·`확인불가` 항목은 분석하지 않는다)
  - 원인은 아래 6개 목록에서만 고르며, 각 항목의 판별 조건은 다음과 같다:

    | 원인 | 판별 조건 |
    | --- | --- |
    | 세입기준 | 세입 계정 수치를 물었는데 세출 계정 수치를 인용했거나, 그 반대인 경우 |
    | 결산기준 | 예산 계정(추경안·예산현액) 수치를 결산 수치(징수결정액 등)로 인용했거나, 그 반대인 경우 |
    | 예산현액기준 | 본예산·추경안과, 이월·전용을 반영한 예산현액을 혼동한 경우 |
    | 총계·순계 차이 | 회계 간 전입·전출을 포함한 총계와 제외한 순계를 혼동한 경우 |
    | 회계연도 차이 | 다른 회계연도의 수치를 인용한 경우 |
    | 기타 | 위 다섯에 해당하지 않으나 문서상 기준 차이가 확인되는 경우 |

  - 근거는 **3문장 이내이면서 전체 200자 이내**의 단답형으로만 작성한다
  - 목록으로 설명되지 않으면 지어내지 말고 `원인 미상`으로 표시한다

### [nice] 3) 중복·유사 질의 통합

- 설명: 위원별로 제출된 질의 중 같은 계정·같은 수치를 묻는 질의를 하나로 묶어, 중복 검증을 줄인다.
- 비고: `prd_lite.md`의 핵심 기능 2가지에는 포함되지 않은 항목이며, 3번의 "중복·유사 질의 통합 30% 이상 감소" 지표와 짝을 맞추기 위해 nice 등급으로 제안한다. must 2가지가 완성된 뒤에만 검토한다.

## 6. 범위와 비범위
*이번 실습에서 만들 것 / 만들지 않을 것*

**범위 (이번에 만들 것)**

- 수치 대조 검증 기능 (5번 must 1)
- 불일치 원인(회계기준) 분석 기능 (5번 must 2)
- 질의서 PDF 업로드 + OLAP 엑셀 업로드 화면, 항목별 판정 결과 표, `검증대상외` 질의 목록
- 결과 표를 클립보드로 복사하거나 CSV로 내려받는 기능 (회신 자료 작성은 사람이 하되, 옮겨 적는 수고는 덜기 위함)
- 파일·형식·용량 오류와 API 오류에 대한 화면 안내
- 실제 D-Brain 자료와 질의사항 원본은 내부망에 있어 반입하지 않으며, `sample-data/` 폴더의 가상 더미데이터로 개발·검증한다

**비범위 (이번에 만들지 않을 것)**

- HWP 파일을 직접 읽는 기능 (질의서는 PDF로 변환해 올리는 것으로 한정)
- 답변서·회신 문서 자동 작성 (검증과 원인 제시까지만, 문서 작성은 사람이)
- D-Brain 직접 연동 및 OLAP 자료 자동 내려받기 (사람이 받아서 업로드)
- 로그인·계정·권한 관리 (혼자 쓰는 도구로 한정)
- 검증 이력 저장(DB) (한 번에 한 건만 처리하고, 새로고침하면 초기화)
- 여러 명 동시 사용 및 결과 공유

## 7. 보안·개인정보 검토
*사용 문서의 공개등급, 개인정보 여부, API 키 관리 방법*

**사용 문서의 공개등급**

- D-Brain OLAP 결산자료와 국회 질의사항 원본은 **내부망 자료**이므로 본 실습 환경에 반입하지 않는다.
- 국회 예산정책처 질의사항은 공개 문서이나, 실습에서는 원문 대신 검증용으로 요약한 텍스트만 사용한다.
- 개발·시연에는 `sample-data/` 폴더의 **가상 더미데이터만** 사용하며, 파일 안에 가상 데이터임을 명시한다.

**파일이 처리되는 위치 (무엇이 밖으로 나가는가)**

- OLAP 엑셀은 **브라우저 안에서만** 읽으며, 서버로 올리지 않는다.
- 질의서 PDF는 iOS Safari/WebKit 브라우저에서 브라우저 내 PDF 파싱이 계속 실패해(브라우저 종류·pdfjs-dist 빌드와 무관하게 재현), 텍스트 추출만 서버(`/api/extract-pdf`, Node.js 런타임)로 옮겼다. 업로드한 PDF 원본은 **그 요청을 처리하는 동안만 서버 메모리에 있고, 디스크·DB에 저장하지 않으며, 응답으로 추출한 텍스트만 돌려주고 즉시 버려진다.**
- 대조 판정(질의 항목 선별, 원자료 대조)은 여전히 브라우저 안에서 계산한다 — 서버는 "PDF → 텍스트" 변환만 담당하고, 그 이후 로직은 그대로 클라이언트에 있다.
- 서버(및 외부 AI API)로 추가로 보내는 것은 **불일치 원인 분석에 필요한 질의 문장 일부와 대조 결과 수치**로 한정한다.

**개인정보 여부**

- 질의서에는 위원명 등 개인 식별정보가 포함될 수 있으므로, 더미데이터에서는 `○○○ 위원` 형태로 익명화한다.
- 이 도구는 개인정보를 **저장하지 않는다.** 다만 원인 분석 과정에서 질의 문장 일부가 외부 AI API로 **전송되므로**, 전송 전에 위원명·직위 등 개인 식별 표현을 제거한 뒤 보낸다.

**API 키 관리 방법**

- `OPENAI_API_KEY`는 프로젝트 폴더의 환경변수 파일에만 보관하고, `.gitignore`에 등록해 저장소에 올라가지 않게 한다.
- 키는 서버 쪽 API 코드에서만 읽어 사용하며, 화면(클라이언트) 코드나 응답에 절대 노출하지 않는다.
- 업로드된 파일은 처리 중에만 사용하고 서버에 저장하지 않는다 (검증 이력 저장은 비범위).

## 8. 기술 스택
*개발에 사용할 도구 모음 (이 과정에서는 Next.js로 고정)*

- **Next.js** — 화면과 서버 쪽 코드(API)를 한 프로젝트에서 처리
- **고정 이유** — Day 1 실습에서 쓴 것과 같은 구성이고, 3일차에 Vercel로 배포할 예정이라 그대로 이어지게 하기 위함
- **AI 모델: `gpt-4o-mini`** — 불일치 원인 분석에만 사용. 새로 고르는 것이 아니라 Day 1 실습에서 이미 정한 모델을 그대로 잇는 것이며, 비용이 가장 적게 든다

## 9. 개발 단위
*핵심 기능을 만들기 좋은 작은 작업 단위로 나눈 목록 (만들 순서대로)*

1. `my-app`에 Next.js 프로젝트 생성 — Day 1과 같은 구성(TypeScript·Tailwind·App Router)
2. 기본 화면 골격 배치 — 업로드 영역 / 검증 실행 버튼 / 결과 표 자리만 잡기
3. OLAP 엑셀 업로드·파싱 — 시트를 읽어 회계·관·항·목·금액 행 목록으로 만들고, 필요한 열이 없으면 어떤 열이 없는지 안내
4. 질의서 PDF 업로드·텍스트 추출 — 브라우저에서 글자를 읽고, 글자 수가 100자 미만이면 추출 실패로 안내
5. 용량·개수 제한 — PDF 50페이지·수치 100건·엑셀 5,000행 상한과 "엑셀은 1개, PDF는 여러 개" 규칙 적용
6. 검증 실행 차단 — 두 파일이 모두 준비 완료여야 검증 버튼이 눌리게 하기
7. 질의 항목 분리 — 질의서 텍스트를 질의 한 건 단위로 쪼개기
8. 검증 대상 수치 추출 — 금액 표기 수치만 뽑고(조·억 혼합 표기 포함) 연도·번호·비율·건수는 제외
9. 대상 계정·인용 기준 추출 — 회계·관·항·목 특정, 예산/결산 기준 판별, 회계 구분이 없으면 후보와 함께 `확인불가`
10. `검증대상외` 분류 — 금액 수치가 없는 질의를 판정에서 빼고 목록에만 남기기
11. 단위 환산 — 조·억·백만·원 혼합 표기를 원 단위로 바꾸고 환산 과정 보관
12. 계정 매칭 — 추출한 계정 표현을 엑셀의 실제 행과 연결
13. 합계·소계 질의 처리 — 상위 계층을 물으면 하위 목을 합산하고 합산 사실·행 수를 표시
14. 대조 판정 — 절대차 10원 기준으로 `일치`/`불일치`/`확인불가` 판정, 근거 위치(회계·관·항·목·행 번호) 부착
15. `확인불가` 과다 경고 — 확인불가 비율 80% 이상이면 "자료가 맞지 않을 수 있음" 경고 표시
16. 판정 결과 표 화면 — 질의별 판정·인용값·원자료값·근거 위치 표시
17. `검증대상외` 목록 화면 — 판정에서 빠진 질의를 따로 보여주기
18. 결과 복사·내보내기 — 결과 표를 클립보드 복사하거나 CSV로 내려받기
19. 불일치 원인 분석 API — 서버에서 `gpt-4o-mini` 호출, 6개 목록의 판별 조건에 따라 선택, 3문장·200자 이내, 설명 불가 시 `원인 미상`
20. 개인 식별 표현 제거 — 서버로 보내기 전 위원명·직위를 지우고 전송
21. 원인·근거를 결과 표에 연결 — `불일치` 행에만 원인과 근거 표시
22. 오류 처리 — 형식·용량·API 오류 시 화면을 비우지 말고 성공한 결과는 남긴 채 실패한 부분만 안내
23. 화면 스타일 적용 — 남색(#003668) 기본, `불일치`는 빨강(#e6002d), PC 1024px 기준
24. 더미데이터로 전체 흐름 검증 — `sample-data`의 질의 1·2가 `일치`·`불일치`와 `결산기준 차이`로 나오는지 확인
25. [nice] 중복·유사 질의 통합 — 같은 계정·수치·기준을 묻는 질의를 하나로 묶기 (must 완료 후에만)
