# PLAN — 결산질의 validator (이번 사이클)

## 이번 사이클 목표
PRD.md에 정의된 두 가지 must 기능(①수치 대조 검증, ②불일치 원인 분석)을 끝까지 동작하는 한 화면으로 완성한다. 질의서 PDF와 OLAP 엑셀을 올리면 항목별 판정(`일치`/`불일치`/`확인불가`) 표와 `검증대상외` 목록이 뜨고, `불일치` 항목에는 회계기준 원인이 붙는다. nice 기능(중복·유사 질의 통합)은 must가 끝난 뒤에만 착수한다.

## 성공 기준
- [ ] `sample-data/`의 질의 1·2를 넣으면 PRD 24번 기준대로 각각 `일치`, `결산기준 차이`로 인한 `불일치`가 정확히 뜬다.
- [ ] 두 파일이 모두 "준비 완료"이기 전에는 검증 버튼이 눌리지 않는다.
- [ ] 판정 표기는 `일치`/`불일치`/`확인불가` 3가지만 쓰이고, 모든 판정에 회계·관·항·목·엑셀 행 번호 근거가 붙는다.
- [ ] 필요 열 누락, 상한 초과(PDF 50p·수치 100건·엑셀 5,000행), PDF 텍스트 추출 실패, API 오류 상황 각각에서 화면이 비지 않고 원인이 안내된다.
- [ ] `불일치` 원인 분석은 `불일치` 항목에만 실행되고, 6개 목록 중 하나 또는 `원인 미상`만 출력되며 200자를 넘지 않는다.
- [ ] `npm run lint`, `npm run build` 통과.

## 작업 목록 (만들 순서대로 — 앞 항목이 뒤 항목을 쉽게 만든다)

1. `app/lib/types.ts`에 질의 항목·판정·OLAP 행 등 공용 타입 정의 — 이후 모든 lib 모듈이 이 타입을 기준으로 맞춰진다.
2. `app/lib/limits.ts`에 상한 상수(PDF 50p·수치 100건·엑셀 5,000행, PDF 텍스트 100자 미만 기준) 정의 — 업로드·파싱 단계에서 바로 참조하도록 먼저 만든다.
3. 기본 화면 골격(`app/page.tsx`) 배치 — 업로드 영역 2개(질의서 PDF, OLAP 엑셀) / 검증 실행 버튼 / 결과 표 자리만 잡는다.
4. `app/lib/excel.ts` — OLAP 엑셀 업로드·파싱, 회계·관·항·목·금액 열 확인, 필요한 열이 없으면 어떤 열이 없는지 반환.
5. `app/lib/pdf.ts` — 질의서 PDF 업로드·텍스트 추출(브라우저), 추출 글자 수 100자 미만이면 실패로 판정.
6. 업로드 단계에 상한(2번) 적용 — 엑셀은 1개, PDF는 여러 개, 초과 시 처리하지 않고 상한 안내.
7. 두 파일 모두 "준비 완료"일 때만 검증 버튼이 눌리도록 화면(3번)에 연결.
8. `app/lib/vocabulary.ts` — 회계·관·항·목 용어 사전과 계정 표현 매칭 기준 정의 — 항목 선별·계정 매칭·엑셀 매칭 모두가 이 사전을 쓴다.
9. `app/lib/select.ts`(1) — 질의서 텍스트를 질의 항목 단위로 분리.
10. `app/lib/amount.ts` — 금액 표기 인식(원·만·억·조 혼합 포함) 및 원 단위 환산, 연도·번호·비율·건수는 금액으로 인식하지 않음.
11. `app/lib/select.ts`(2) — 검증 대상 수치 추출(10번 활용), 대상 계정을 8번 사전으로 특정, 회계 구분 불명 시 `확인불가`+후보 계정 표시, 인용 기준(예산/결산, 미표기 시 `기준 미표기`) 함께 추출.
12. `app/lib/select.ts`(3) — 금액 수치가 없는 질의를 `검증대상외`로 분류해 목록에는 남기되 판정 대상에서 제외.
13. `app/lib/verify.ts`(1) — 11번에서 특정된 계정을 4번이 파싱한 엑셀 행과 연결(계정 매칭).
14. `app/lib/verify.ts`(2) — 상위 계층(회계·관·항) 합계 질의는 하위 목을 합산해 비교, 합산 사실과 사용 행 수를 함께 기록.
15. `app/lib/verify.ts`(3) — 절대차 10원 기준 `일치`/`불일치`/`확인불가` 판정, 회계·관·항·목·엑셀 행 번호 근거 부착.
16. `app/lib/verify.ts`(4) — 선별된 항목 중 `확인불가` 비율 80% 이상이면 "원자료가 질의서와 맞지 않을 수 있음" 경고 플래그 반환.
17. 판정 결과 표 화면 — 3번 골격에 질의별 판정·인용값·원자료값·근거 위치, 16번 경고 배너 연결.
18. `검증대상외` 목록 화면 — 12번 결과를 별도 목록으로 표시.
19. 결과 복사·내보내기 — 결과 표를 클립보드 복사 / CSV 다운로드 버튼 추가.
20. `app/api/analyze/route.ts`(신규) — 서버에서 `gpt-4o-mini` 호출, `불일치` 항목에만 실행, 6개 원인 목록 중 판별 조건에 맞는 것 선택(없으면 `원인 미상`), 3문장·200자 이내로 제한.
21. 20번 호출 전 개인 식별 표현(위원명·직위 등) 제거 로직 추가 — 서버로 텍스트를 보내기 직전 단계에 삽입.
22. 원인 분석 결과를 17번 결과 표의 `불일치` 행에만 연결해 표시.
23. 오류 처리 — 형식·용량·API 오류 시에도 이미 성공한 결과는 남기고 실패한 부분만 화면에 안내(4~22번 전 구간에 오류 경로 점검).
24. 화면 스타일 적용 — 남색(#003668) 기본, `불일치` 강조 빨강(#e6002d), PC 1024px 기준으로 17~19번 화면 다듬기.
25. `sample-data/`로 전체 흐름 검증 — 질의 1·2가 성공 기준의 `일치`/`결산기준 차이`로 정확히 나오는지 수동 확인.
26. [nice, must 완료 후에만] 중복·유사 질의 통합 — 같은 계정·수치·기준을 묻는 질의를 하나로 묶어 검증 대상 항목 수를 줄인다.
