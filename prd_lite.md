# 결산질의 validator 서비스 기획서 (PRD_lite)

- 작성자: 정민규
- 작성일: 2026-08-03

---

## 1. 이 앱은 한 마디로 무엇인가요?
- 답변: 국회(예산정책처·예산결산위원회)의 국가결산 관련 질의서와 D-Brain에서 내려받은 OLAP 자료를 함께 올리면, 질의서에 인용된 수치가 실제 결산 수치와 맞는지 자동으로 대조해 오류를 찾아주고, 수치가 다를 경우 어떤 회계기준(세입기준·결산기준 등)의 차이 때문인지 단답형 근거까지 제시해 주는 결산 수치 검증 도구

---

## 2. 누가 쓰고, 왜 쓰나요? (딱 한 줄씩만!)
- 누가 쓰나요?
  - 답변: 재정경제부 회계결산과처럼 국회 질의에 대응해 결산 수치를 검증해야 하는 부처 실무 담당자
- 어떤 불편을 해결하나요?
  - 답변: 질의서에 인용된 수치가 맞는지 확인하려면 OLAP 엑셀에서 피벗테이블을 일일이 돌려 대조해야 해서 시간이 오래 걸리고, 수치가 어긋나도 그 원인이 회계기준 차이인지 단순 오류인지 파악하기 어렵다

---

## 3. 개발할 핵심 기능 (딱 2가지만!)
> 💡 너무 많은 기능을 만들려고 하면 AI가 코드를 꼬아버립니다.
> 가장 중요한 2개 기능만 "규칙"을 정해 알려주세요.

### 1) 수치 대조 검증
- 설명: 질의서에 인용된 수치와 OLAP 자료의 실제 수치를 항목별로 자동 대조해, 일치 여부를 표로 보여준다.
- AI가 꼭 지켜야 할 규칙:
  - 질의서는 PDF, OLAP 자료는 엑셀 파일로 각각 업로드하며, 두 파일이 모두 올라오기 전에는 검증을 시작하지 않는다
  - 판정은 `일치` / `불일치` / `확인불가` 3가지로만 표시하고, 그 외 표현은 쓰지 않는다
  - OLAP 자료에서 해당 수치를 찾지 못하면 추정하지 말고 반드시 `확인불가`로 표시한다
  - 단위(억원·백만원·원)가 다르면 원 단위로 환산해 비교하고, 환산 과정을 함께 표시한다
  - 일의 자리 차이(10원 미만)는 절사·반올림에 따른 오차로 보고 `일치`로 판정하며, 10원 이상 차이 나면 `불일치`로 판정한다
  - 모든 판정에는 근거 위치(시트명·항목명·행)를 반드시 함께 표시한다

### 2) 불일치 원인(회계기준) 분석
- 설명: `불일치`로 판정된 항목만 골라, 어떤 회계기준 차이 때문에 수치가 어긋났는지 단답형 근거를 제시한다.
- AI가 꼭 지켜야 할 규칙:
  - `불일치`로 판정된 항목에 대해서만 실행한다 (`일치`·`확인불가` 항목은 분석하지 않는다)
  - 원인은 미리 정한 목록에서만 고른다: 세입기준 / 결산기준 / 예산현액기준 / 총계·순계 차이 / 회계연도 차이 / 기타
  - 근거는 3문장 이내의 단답형으로만 작성한다
  - 목록으로 설명되지 않으면 지어내지 말고 `원인 미상`으로 표시한다

---

## 4. 이번에 절대 만들지 않을 기능 (욕심 버리기)
> 💡 AI에게 미리 "이건 안 만들 거야"라고 선언해야 엉뚱한 코딩을 안 합니다.
- HWP 파일을 직접 읽는 기능 (질의서는 PDF로 변환해 올리는 것으로 한정)
- 답변서·회신 문서 자동 작성 (검증과 원인 제시까지만, 문서 작성은 사람이)
- D-Brain 직접 연동 및 OLAP 자료 자동 내려받기 (사람이 받아서 업로드)
- 로그인·계정·권한 관리 (혼자 쓰는 도구로 한정)
- 검증 이력 저장(DB) (한 번에 한 건만 처리하고, 새로고침하면 초기화)
- 여러 명 동시 사용 및 결과 공유

---

## 5. 디자인 느낌과 색상 고르기
- 전체적인 분위기: 정부 부처 실무 도구답게 신뢰감 있고 차분한 톤. 장식보다 표와 수치가 한눈에 들어오는 것을 최우선으로 한 깔끔한 업무용 화면
- 메인 색상: 재정경제부 로고에서 추출한 남색(#003668) 계열을 기본으로 사용하고, `불일치` 항목만 로고의 빨강(#e6002d)으로 강조 (기존 프로필 페이지와 색상 통일)
- 화면 크기 제약: 엑셀 표를 다루는 도구이므로 PC 화면 우선(최소 너비 1024px 기준), 모바일 대응은 하지 않음
