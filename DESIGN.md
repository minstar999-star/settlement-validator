# DESIGN — 결산질의 validator

> PLAN.md의 26개 작업을 실제로 어떻게 배치·연결할지 정리한 설계 문서.

## 1. 화면 구성 (무엇이 어디에 보이는지)

한 페이지(`app/page.tsx`)에 위에서 아래로 배치. PC 1024px 기준, 기본색 남색(`#003668`), `불일치`만 빨강(`#e6002d`)으로 강조.

```
┌─────────────────────────────────────────────┐
│ ① 업로드 영역 (좌우 2분할)                    │
│  [질의서 PDF 업로드]   [OLAP 엑셀 업로드]      │
│  - 여러 개 가능         - 1개만              │
│  - 상태: 준비중/준비완료/오류 뱃지 표시        │
├─────────────────────────────────────────────┤
│ ② [검증 실행] 버튼                            │
│  - 두 파일 모두 "준비완료"가 아니면 비활성화   │
├─────────────────────────────────────────────┤
│ ③ 확인불가 과다 경고 배너 (조건부 노출)        │
│  - "원자료가 질의서와 맞지 않을 수 있습니다"    │
├─────────────────────────────────────────────┤
│ ④ 판정 결과 표                                │
│  질의 항목 | 판정 | 인용값 | 원자료값 |         │
│  근거 위치(회계·관·항·목·행) | 원인(불일치만)   │
│  - 판정별 뱃지 색: 일치=회색, 불일치=빨강,      │
│    확인불가=노랑                              │
│  [결과 복사] [CSV 다운로드] [보고서 생성] 버튼  │
├─────────────────────────────────────────────┤
│ ⑤ 검증대상외 질의 목록 (별도 접이식 섹션)       │
│  - 금액 수치가 없는 질의를 그대로 나열          │
└─────────────────────────────────────────────┘
```

- 업로드 전(①만 있는 상태)에는 ②~⑤는 비어있는 안내 문구만 표시.
- 오류(형식·용량·API)는 화면을 비우지 않고, 이미 나온 결과는 그대로 둔 채 실패한 부분에만 인라인 오류 메시지를 붙인다.

## 2. 데이터 흐름 (입력 → 처리 → 출력)

**핵심 원칙 (PRD 7번, 2026-08-04 갱신): OLAP 엑셀은 브라우저 밖으로 나가지 않는다. 질의서 PDF는 iOS Safari/WebKit 호환성 문제로 텍스트 추출만 서버를 거치되, 저장하지 않고 요청 처리 중에만 메모리에 둔다.** 그 외 서버로 가는 것은 ⑥ 단계, "불일치로 판정된 질의 문장 일부 + 대조 수치"뿐이다.

```
① 파일 업로드
   질의서 PDF(여러 개) + OLAP 엑셀(1개)
        │
② 파싱 (PDF만 서버를 거친다)
   pdf.ts → fetch POST → app/api/extract-pdf/route.ts (Node.js, pdfjs-dist)
            → PDF 텍스트 추출 (100자 미만이면 실패 처리) → 텍스트만 응답, 원본은 버림
   excel.ts → 브라우저 안에서 엑셀 행 목록 [회계, 관, 항, 목, 금액] 직접 파싱 (필요 열 없으면 안내)
        │
[여기부터는 그대로 브라우저 안에서만 처리]

③ 질의 항목 처리 (select.ts + amount.ts + vocabulary.ts)
   질의 항목 분리 → 금액 표기 인식·환산 → 대상 계정 특정
   → 검증대상 수치 / 검증대상외 로 분리
        │
④ 대조 판정 (verify.ts)
   계정 매칭(엑셀 행 연결) → 필요 시 하위 목 합산
   → 절대차 10원 기준 일치/불일치/확인불가 판정
   → 근거 위치(회계·관·항·목·행) 부착
        │
⑤ 화면 렌더링
   판정 결과 표 + 검증대상외 목록 + 확인불가 과다 경고

[여기서부터만 서버로 전송 — 6~7단계]

⑥ 불일치 항목만 골라 개인 식별 표현(위원명·직위) 제거
        │  fetch POST
⑦ app/api/analyze/route.ts (서버)
   OpenAI gpt-4o-mini 호출 → 6개 원인 목록 중 판정 또는 `원인 미상`
   (3문장·200자 이내로 제한)
        │  응답
⑧ 결과 표의 해당 `불일치` 행에 원인 병합 표시

[출력]
결과 복사(클립보드) / CSV 다운로드 / 보고서 생성(PDF) — 모두 브라우저 안에서 완결.
보고서는 불일치 항목만 요약해 화면 밖에 그려둔 DOM을 이미지로 캡처해 PDF에 붙이는
방식(html2canvas-pro + jsPDF)이라 서버로 아무것도 보내지 않는다.
```

## 3. 기술 선택

**기본: Next.js (App Router)** — PRD·CLAUDE.md에서 고정한 스택. 화면(프론트)과 서버 코드(API)를 한 프로젝트로 만들 수 있어서, 오늘 그대로 Vercel에 올리면 별도 서버 없이 배포가 끝난다.

이미 설치된 것 외에 추가로 쓰는 기술은 없고, 기존에 정해둔 것들만 그대로 사용한다:

| 기술 | 쓰는 곳 | 쉬운 설명 |
| --- | --- | --- |
| **React / TypeScript** | 화면 전체 | Next.js 안에 기본 포함된 화면 그리는 도구. 이미 설치돼 있어 추가 설치 불필요. |
| **Tailwind CSS** | 화면 스타일 | 색상·간격 같은 디자인을 코드에 바로 쓰는 방식. 이미 설치돼 있음. |
| **pdfjs-dist** | ② PDF 텍스트 추출 | PDF를 열어 글자만 뽑아내는 도구. iOS Safari 호환 문제로 브라우저 대신 서버(`/api/extract-pdf`)에서 돌림 — 원본 파일은 저장 없이 그 요청 동안만 씀. 이미 설치돼 있음. |
| **read-excel-file** | ② 엑셀 파싱 | 브라우저 안에서 엑셀 파일을 표(행·열)로 읽는 도구. 이미 설치돼 있음. |
| **OpenAI API (`gpt-4o-mini`)** | ⑦ 원인 분석 | 문장을 넣으면 답을 만들어주는 AI 서비스. `env.txt`의 키로 서버에서만 호출. 새로 설치할 것 없이 `fetch`로 바로 호출. |
| **CSV 내려받기** | ④ 결과 내보내기 | 표를 텍스트로 바꿔 파일로 저장하는 방식. 별도 라이브러리 없이 코드 몇 줄로 직접 만듦 (설치 불필요). |
| **클립보드 복사** | ④ 결과 복사 | 브라우저에 이미 내장된 "복사하기" 기능(`navigator.clipboard`). 설치 불필요. |
| **html2canvas-pro + jsPDF** | ④ 보고서 생성(PDF) | 화면 밖에 그려둔 요약 문서를 이미지로 캡처해 PDF에 붙이는 도구. 한글은 표준 PDF 폰트가 없어 폰트를 심는 대신 화면을 그대로 캡처하는 방식을 씀. Tailwind v4 기본 색상이 `oklch()`라 이를 못 읽는 일반 `html2canvas` 대신 그 색을 지원하는 `html2canvas-pro`를 씀. |
| **Vercel** | 배포 | Next.js를 만든 회사가 운영하는 배포 서비스. `vercel` 명령 한 번이면 오늘 만든 화면이 인터넷 주소로 바로 열림. `env.txt`의 `VERCEL_TOKEN`으로 인증해 CLI로 배포. |

- 보고서 생성(PDF) 기능을 위해 `jspdf`, `html2canvas-pro`를 새로 설치했다. 그 외 추가 설치는 없다.
- 로그인·데이터베이스·다중 사용자 관련 기술은 PRD 비범위이므로 의도적으로 넣지 않는다.
